{
  "name": "vpngateway-gui",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "web",
    "api",
    "proxy"
  ],
  "scripts": {
    "build": "npm run build -w proxy && npm run build -w api && npm run build -w web",
    "test": "npm run test -w proxy && npm run test -w api && npm run test -w web",
    "export-openapi": "npm run export-openapi -w api",
    "generate:api-client": "npm run export-openapi && npm run generate:api-client -w web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
