# 対話・ドキュメントの言語

- 対話・コメント・エラーメッセージの解説・ドキュメント（README等）に用いる言語は、プロジェクトで定めた言語に統一し、同一責務の説明で複数言語を混在させないこと。

# README（利用者向け）

- `README.md`はエンドユーザー・運用者（このシステムを導入・運用する人）が読む文書であり、`specs/`（開発者向けの要件・設計）とは読者が異なる。README単体で導入・運用の手順が完結するように書くこと。`specs/`や`wbs/`へのリンクは、より詳しい設計判断を知りたい場合の参照先として添えるに留め、手順そのものを設計書に委ねない。
- 本文は「だ・である」調で統一し、口語的な表現（例: 「やりたいこと」）を用いないこと。
- 細かい動作原理（内部で何がどう処理されるかの詳細）は記載しないこと。コマンドの使い方・引数・引数が取りうる値・その設定が意味することを簡潔に述べれば足りる。内部動作の詳細を知りたい読者は`specs/`を参照させる。
- 章構成は、読者が上から順に読み進めれば導入から運用まで理解できるように、概要・インストール方法・設定方法・注意事項の順を基本とすること（章名はこれに限らないが、この順序・粒度を踏まえる）。
- 「既知の制約」のように読者が事前に知るべき制約・対応範囲は、読者がこのアプリを詳しく知らない前提で、表形式など一覧しやすい形で示すこと。
- READMEの本文に、README自体の執筆方針・スコープ・「このREADMEだけで完結する」といった自己言及的な注釈文を書かないこと（読者にとって無意味な情報であるため）。そうした方針はこのAGENTS.mdなど、開発者・エージェント向けの文書にのみ記載する。

# 仕様駆動開発（Spec-Driven Development）

- 本プロジェクトは仕様駆動開発を採用し、要件・設計・タスクを `specs/` 配下に文書化する。実装に着手する前に、対象となる要求・決定事項を該当ファイルへ反映すること。実装と記述が乖離した場合は都度更新する。
- サービス全体に関する要件・設計・タスクは `specs/` 直下に配置する。
- サービスを構成するアプリケーション（実行単位ごとのサーバー・クライアント等）ごとに `specs/<アプリケーション名>/` ディレクトリを作成し、同様のファイル構成を配置する。
- **全体用（`specs/`直下）と個別アプリケーション用（`specs/<アプリケーション名>/`）の書き分け**: 全体用の`design.md`には、複数アプリケーションにまたがる決定（どのアプリケーションが何を担うか、アプリケーション間で交わす契約の要点、契約を変更した場合はその理由）までを書き、各契約の実装詳細（具体的なエンドポイント一覧・リクエスト/レスポンス形状・ファイル名・使用アルゴリズム等）は、それを実装するアプリケーションの`specs/<アプリケーション名>/design.md`に書く。複数アプリケーションにまたがる機能（例: 認証、ベンダー選択）を設計するときも、全体設計は「何を実現するためにどう役割分担するか」の要約と各アプリケーションのdesign.mdへの参照に留め、同じ詳細を全体設計と個別設計の両方へ書き写さない（個別設計を一次情報とする。全体設計は要約＋参照）。requirements.mdも同様に、全体は利用者から見た要求（何が達成されるべきか）、個別アプリケーションはそのアプリケーションが担う技術的な要求までとし、両者でほぼ同じ文を繰り返さない。
- 既存の`specs/`を修正・追記する作業に着手する前に、書こうとしている内容が全体用か個別アプリケーション用かを都度判定し、正しい方へ書くこと（判断に迷う場合は、影響が1アプリケーションに閉じるか複数にまたがるかで判定する）。追記後、同じ内容を他方へもほぼそのまま書き写していないか見直す。
- ディレクトリ（サービス全体・各アプリケーションのいずれも）には以下の3ファイルを置き、記載範囲を混在させないこと。
  - `requirements.md`（要件定義）: 何を実現するか（目的・要求・制約・非機能要件）を記載する。具体的な実装手段（技術選定、データ構造、API仕様等）には踏み込まない。
  - `design.md`（基本設計・詳細設計）: どう実現するかを記載する。アーキテクチャ、コンポーネント間の責務分担、インターフェース、データ構造、技術選定とその理由を含む。
  - `tasks.md`（タスク一覧）: 実施すべき作業項目・未着手の課題を記載する。

# コミット規則（phaseごとのcommit）

- WBS（`wbs/`）のphaseごとにcommitを作成する。あるphaseの変更は、そのphaseの完了時（実装完了・検証完了の各区切り）にcommitし、複数phaseの変更を同一のcommitに混在させない。
- 前のphaseの変更が未コミットのまま次のphaseの作業に入らない。着手前に、直前のphaseまでの変更をcommitしておく。
  - 理由: 同一ファイルへ複数phaseの変更が未コミットのまま重なると、後からphase単位に分割できなくなる（2026-09-21、当時の番号でphase3・5・8（2026-09-22のWBS正規化後はphase3・4・5）が未コミットで混在し、ファイル単位の粗い分割にしかできず、当時のphase3・5（正規化後のphase3・4）のcommit単体ではビルド・テストが通らなくなった）。
- **1つのphaseの変更は、実機検証まで完了してから1つのcommitにまとめる。「実装完了・検証待ち」のような検証未了の状態でcommitを作らない（2026-09-22改訂）。** 検証で不具合を見つけて修正した場合も、修正・再検証まで終えてから1つのcommitにする（実装commit→検証commitと分割しない）。commitメッセージの状態欄（`<状態>`）は「実装完了・検証完了」または、その区切りに応じた表現（例: 不具合修正を伴えば「不具合修正・再検証完了」）を使う。やむを得ず検証が完了しないまま作業を中断する場合は、次のphaseの作業には進まず、未コミットのまま同一phase内で作業を再開する（前項のphase混在防止の理由による）。
- 上記は実装（コード変更）を伴うphaseが対象であり、次項の設計書のみの修正には適用しない。
- **実装（コード変更）を伴わない設計書（`specs/`）の修正（要件の追加、既存フェーズの実施中に判明した設計の見直し等）も、実装の着手を待たず、その修正の区切りごとに単独でcommitを作成する。** 設計検討だけで終わるセッションでも、その内容を未コミットのまま次のセッション・次の作業へ持ち越さない。
- commitメッセージの1行目は`phaseNの<状態>（<内容>）`の形式とする（例: `phase5の実装完了・検証完了（接続先選択UIの刷新）`）。phaseに属さない作業（規則・ドキュメントの整備等）は、phase名を付けず内容を書く。本文には、変更の要点と検証結果（未検証の範囲を含む）を書く。
- 各commitは、可能な限り単体でビルド・テストが通る状態にする。
- `git push`は、利用者から明示的な許可があるときのみ行う。

# 実機検証のタイミング

- 実機での検証は、フェーズ内のタスク・機能ごとに個別に行わず、そのフェーズの主要タスクの実装がすべて完了した最後にまとめて実施する。実装の合間に都度実機確認を挟まない（手戻りが分かった時点でまとめて直せるようにするため）。
- ここでいう「実機」とは、開発者が用意する検証用環境（検証用サーバ等）を指す。Raspberry Pi等の実運用相当ハードウェアを意味するものではなく、実運用相当ハードウェアでの検証は別途明記された場合にのみ行う。

# ベンダー非依存（VPNプロバイダに依存しない）

- 本システムはVPNベンダーに依存しない（`specs/requirements.md`「ベンダー非依存性」）。本番のソースコード（`api/src`・`proxy/src`・`web/src`・`web/server`・`install/`・composeの本体・共通のDockerfile）に、ベンダーのID・名称・CLIの書式・エラー文言を書かない。**コメントやエラーメッセージの例にも書かない。**
- ベンダー間の差が必要なときは、コードに分岐を足さず、プロファイル（`vendors/<ID>/profile.json`）の項目として抽象化する。分岐は「プロファイルが宣言する機構」（例: `loginMethod`）に対してのみ行い、ベンダーには行わない。ベンダー固有になりうる項目に暗黙の既定値を置かず、プロファイルで必須にする。
- ベンダー固有のものは、ベンダーバンドル（`vendors/<ID>/`）に置く。ベンダーの追加・削除がバンドルの追加・削除だけで済まなくなる変更をしない。
- 仕様書・WBS・E2E・単体テスト・バンドルの中は、ベンダー固有の情報を置いてよい。中立性の検査（`scripts/check-vendor-neutrality.mjs`。`npm test`に含まれる）が本番コードの違反を検出する。

# lib / util の使い分け

- 汎用ヘルパーとドメイン知識に依存する処理は、置き場を分離すること。判定基準（ポータビリティテスト）:
  - プロジェクト固有のドメイン型や、プロジェクト固有モジュールを一切importしない
  - 入出力がプリミティブ値（string/number/boolean等、言語の基本型）のみで完結する
  - 全く別のプロジェクトに貼り付けても意味が通じる
- 上記に当てはまらない、プロジェクトのドメイン知識に依存する処理は、責務ごとにサブディレクトリへ分類して配置すること。
- 新規ファイルを追加する際は、まずポータビリティテストで汎用ヘルパー行きかを判定し、ドメイン知識に依存する場合は既存のサブディレクトリに合致する責務があればそこに配置し、無ければ新しい責務ディレクトリを作成すること。

# API設計原則

- APIのパスは操作（動詞）を示さないこと。操作の意味は HTTP メソッド（GET/POST/PUT/DELETE等）で表現する。
  - 悪い例: `POST /orders/{id}/cancel`（`cancel` という操作をパスに埋め込んでいる）
  - 良い例: `DELETE /orders/{id}`（「取り消す」という操作の意味は DELETE メソッドに委ねる）
- APIのパスは、リソースが何であるかを明確に特定できるものとすること。曖昧・抽象的すぎる語は避けること。
  - 悪い例: `GET /summary`（`summary` は概念であり、抽象度が高い）
  - 良い例: `GET /orders/{id}/items`

# 命名規則

- 識別子の種別（変数・関数、型、ファイル、設定キー等）ごとに命名規則を一貫させること。使用する言語・ツールのエコシステムにおける慣用的な規約（スタイルガイド）に従うこと。

# コーディング規則

- 複数の引数が暗黙的に渡される高階関数（コールバックを受け取る組み込み関数等）へは、関数参照を直接渡さないこと。無名関数（ラムダ）でラップし、呼び出す関数に渡す引数を明示すること。
  - 悪い例: `items.map(toShareFile)`（`toShareFile` に index や配列自体が暗黙的に渡ってしまう）
  - 良い例: `items.map((item, index) => toShareFile(item, index))`

# コード理解負債改善規定

- コード負債改善のため、コードの理解を助けるためのコメントを記載すること。
- 自動生成コード以外のコードに適応すること
- ファイル先頭に、そのファイルの責務と処理概要を記載すること。
- 関数・メソッドごとに以下をコメントで明示すること。
  - 何をする処理か（目的）
  - 入力（Input）
  - 出力（Output）
  - 使用例（Example）
- `unknown` や外部入力を扱う処理では、期待する入力形状（最小要件）を明示すること。
- 分岐やループが複雑なブロック（目安: ネスト2段以上、または条件分岐3つ以上）には、
  「何を検証・除外・抽出しているか」の意図コメントを記載すること。

## 推奨ルール

- 失敗時の方針（`undefined` を返す、早期 return する、など）をコメントに含めること。
- 副作用を伴う処理（状態更新、外部I/O、リソース解放など）は理由をコメントで補足すること。
- 実装上の制約（実行環境の制約、API互換性要件など）を必要に応じて明記すること。

## 禁止ルール

- コードを見れば自明なコメント（例: 代入、単純インクリメントの説明）は記載しないこと。
- 実装と乖離した古いコメントを放置しないこと。
